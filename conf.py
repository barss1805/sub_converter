# variable to extention subtitles
subtitle_extention = "srt"
# variable to input language(used to change the old filename)
input_lang = "en_US"

# variable for the target language(used in generating a new filename)
change_lang = "ru_RU"
# variable for specifying the target language in the translator
target_to_translate = "ru"
